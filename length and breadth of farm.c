#include<stdio.h>
int main()
{
float length,breadth,area;
printf("Enter the length and breadth value of farm:");
scanf("%f%f",&length,&breadth);
area=length*breadth;
printf("\n %f 0.5",area);
return 0;
}
